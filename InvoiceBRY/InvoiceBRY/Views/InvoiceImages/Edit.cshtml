@model InvoiceBRY.Models.InvoiceImage
@{
    ViewData["Title"] = "Create";
}

<h1>Create</h1>

<h4>Invoice Image</h4>
<h4 class="text-danger">@ViewData["DANGER"]</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit" enctype="multipart/form-data" >
            <div class="form-group">
                <input asp-for="Image" class="form-control" type="file" width="100">
            </div>
            <div class="form-group">
                <input type="submit" value="Create" asp-route-id="@ViewData["imageId"]" asp-route-invoiceId="@ViewData["invoiceId"]" asp-route-name="@ViewData["name"]" asp-route-subcontractorId="@ViewData["subcontractorId"]" asp-route-HST="@ViewData["HST"]" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index" asp-route-invoiceId="@ViewData["invoiceId"]" asp-route-name="@ViewData["name"]" asp-route-subcontractorId="@ViewData["subcontractorId"]" asp-route-HST="@ViewData["HST"]">Back to Invoice(DOESN'T WORK)</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
