@model InvoiceBRY.Models.InvoiceDescription

@{
    ViewData["Title"] = "Delete";
}

<h1>Delete</h1>

<h3>Are you sure you want to delete this?</h3>
<div>
    <h4>InvoiceDescription</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.DescriptionId)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.DescriptionId)
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.InvoiceId)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.InvoiceId)
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Description)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Description)
        </dd>
    </dl>

    <form asp-action="DeleteSubcontractor">
        <input type="hidden" asp-for="DescriptionId" />
        <input type="submit" value="Delete" asp-route-subcontractorId="@ViewData["subcontractorId"]" asp-route-name="@ViewData["name"]" asp-route-HST="@ViewData["HST"]" class="btn btn-danger" /> |
        <a asp-action="SubcontractorIndex" asp-route-invoiceId="@ViewData["invoiceId"]" asp-route-name="@ViewData["name"]" asp-route-subcontractorId="@ViewData["subcontractorId"]" asp-route-HST="@ViewData["HST"]">Back to List</a>
    </form>
</div>
